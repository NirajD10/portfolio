import React from "react";

const ProjectsHeadingTitle = ({ children }) => {
  return <h2 className="font-dm_sans font-bold text-[2rem] text-dark-purple">{children}</h2>;
};

export default ProjectsHeadingTitle;
